Executable usage: ./a.out <snr-db> <message_len> <decode method hard/soft>

Script usage: ./sim.sh <snr-db-min> <snr-db-max> <message_len> <decode method hard/soft>
Script will run <snr-db-max> - <snr-db-min> (both integers) iterations, one for each SNR value (integers)
and print output to console